# AI-Powered Document & Image Processor (Production-like MVP)

## 🏗️ Архитектурное решение: Модульный монолит
**Почему не микросервисы:**
- Проект разрабатывается **одной командой** (1 разработчик)
- Количество core endpoint'ов **менее 5**
- Требуется **быстрая разработка** MVP (3 недели)
- Нет требований к **независимому масштабированию** компонентов

**Преимущества выбранной архитектуры:**
- **Сокращение времени разработки на 40%** (относительно микросервисов)
- **Простота отладки и тестирования** (все компоненты в одном процессе)
- **Лёгкий деплой** (один контейнер на Cloud Run)
- **Возможность эволюции:** Чёткое разделение на модули позволяет при необходимости выделить сервисы в будущем

## 🎯 Core Features
1.  **Аутентификация:** "Sign in with Google" (Firebase OAuth).
2.  **Обработка PDF:** Нумерация, сжатие, объединение.
3.  **Раскраска изображений:** Через Replicate API (`colorful.image-colorization`).
4.  **API Документация:** Полное Swagger/OpenAPI описание.

## 🛠️ Tech Stack (конкретные версии)
- **Frontend:** Angular 21, кастомный дизайн, нативный drag-and-drop.
- **Backend:** **NestJS v11 (модульный монолит)**, Firebase Admin SDK, `pdf-lib`, `sharp`.
- **Auth:** Firebase Auth (только Google Provider).
- **Документация:** `@nestjs/swagger`, Swagger UI.
- **Инфраструктура:** Cloud Run, Firebase Hosting, Cloud Storage.

## 📁 Структура проекта (модульный монолит с shared типами)
ai-file-processor/
├── frontend/ # Angular 21 app
│ └── src/app/
│ ├── core/
│ ├── modules/
│ └── shared/ # ! Только фронтенд-специфичные shared-компоненты
├── backend/ # NestJS модульный монолит
│ ├── src/
│ │ ├── modules/
│ │ └── shared/ # ! Только бэкенд-специфичные shared-утилиты
├── shared/ # ! ОБЩИЕ ТИПЫ между фронтом и бэком
│ ├── dto/ # Data Transfer Objects
│ │ ├── auth.dto.ts # LoginResponse, UserProfile
│ │ ├── file.dto.ts # FileUploadResponse, ProcessingResult
│ │ └── api.dto.ts # Pagination, ApiResponse<T>
│ ├── interfaces/ # Общие интерфейсы
│ │ ├── user.interface.ts
│ │ └── processing.interface.ts
│ └── constants/ # Общие константы
│ └── api.constants.ts
└── PROJECT_IDEA.md

## 🚀 План (3 недели)
**Неделя 1: Foundation**
- День 1-2: Инициализация репозитория, Angular 21 + NestJS проекты.
- День 3: Настройка Firebase Project (Google OAuth, Hosting).
- День 4: Модуль аутентификации (Firebase Guard + Swagger декораторы).
- День 5: Drag-and-drop компонент на фронтенде.

**Неделя 2: Business Logic**
- День 6-7: PDF модуль (NestJS + `pdf-lib`), Swagger документация.
- День 8-9: Image модуль (NestJS + Replicate API), Swagger документация.
- День 10: Интеграция фронтенд-бэкенд, прогресс-бары.

**Неделя 3: Polish & Deploy**
- День 11-12: Доработка Swagger (примеры, описания ошибок, security schemes).
- День 13: UI улучшения (сравнение до/после, адаптивность).
- День 14: Деплой на Cloud Run (бэкенд) и Firebase Hosting (фронтенд).
- День 15: Финальное тестирование, подготовка README с архитектурным решением.

## 🔧 Production-элементы (без overengineering)
1.  **Swagger Documentation:** Полное описание API с автогенерацией + ручные описания.
2.  **Модульная структура:** Чёткое разделение кода (легко вынести в микросервисы при необходимости).
3.  **Centralized Configuration:** Конфигурация через environment variables.
4.  **Error Handling:** Единый формат ошибок для фронтенда.
5.  **Security:** Firebase JWT валидация, CORS, rate limiting (опционально).

## 📊 Итог для резюме
**Ключевые достижения для CV:**
- "Спроектировал **модульный монолит на NestJS**, выбрав оптимальную архитектуру для MVP"
- "Реализовал полный цикл аутентификации через **Firebase Auth (Google OAuth)**"
- "Интегрировал **Swagger/OpenAPI документацию** для всех endpoint'ов"
- "Развернул production-окружение на **Google Cloud (Cloud Run + Firebase Hosting)**"
- "Достиг **80%+ покрытия кода** unit-тестами"

---

**Next Step:** Создать Firebase проект и сгенерировать базовую структуру модулей через DeepSeek.